# Banco de dados (para desenvolvimento local use localhost; no Docker use mysql)
DATABASE_URL="mysql://vital:vital_secret@localhost:3306/vital"

# Segredo para assinatura dos JWTs (gere um valor longo e aleatório em produção)
JWT_SECRET="change-me-in-production-use-long-random-string"

# Nome do cookie onde o JWT será armazenado
AUTH_COOKIE_NAME="vital_token"

# LawX SSO: segredo compartilhado com o LawX (mesmo valor definido no LawX)
CENTRALIA_SSO_SECRET=""
